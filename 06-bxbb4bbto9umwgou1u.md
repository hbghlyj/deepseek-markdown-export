### Решение функционального уравнения
\[
y(x) \, y(x+1) + a \, \bigl[ y(x+1) - y(x) \bigr] = 0,
\]
где \( a \) — свободный параметр.

---

### 1. Случай \( a \neq 0 \)

Уравнение преобразуется к виду:
\[
y(x+1) \bigl( y(x) + a \bigr) = a \, y(x).
\]
Если \( y(x) + a \neq 0 \), то
\[
y(x+1) = \frac{a \, y(x)}{y(x) + a}.
\]

#### 1.1. Нетривиальные решения (\( y(x) \not\equiv 0 \))
С помощью замены \( z(x) = \dfrac{1}{y(x)} \) получаем:
\[
z(x+1) = z(x) + \frac{1}{a}.
\]
Общее решение этого разностного уравнения:
\[
z(x) = \frac{x}{a} + h(x),
\]
где \( h(x) \) — произвольная периодическая функция с периодом \( 1 \), то есть \( h(x+1) = h(x) \). Тогда
\[
y(x) = \frac{1}{z(x)} = \frac{a}{x + a \, h(x)}.
\]

**Замечание:** При таком построении автоматически выполняется \( y(x) \neq 0 \) и \( y(x) \neq -a \), что согласуется с предположениями.

#### 1.2. Тривиальное решение
Непосредственной подстановкой проверяется, что \( y(x) \equiv 0 \) также является решением при любом \( a \).

---

### 2. Случай \( a = 0 \)

Уравнение принимает вид:
\[
y(x) \, y(x+1) = 0 \quad \text{для всех } x.
\]
Это означает, что для каждого \( x \) хотя бы одно из значений \( y(x) \) или \( y(x+1) \) равно нулю. Общее решение — произвольная функция \( y(x) \), удовлетворяющая условию:
\[
y(x) \cdot y(x+1) = 0 \quad \forall x.
\]
Например, функция может быть нулевой на всей оси или принимать ненулевые значения только на непересекающихся интервалах длины не более 1, отстоящих друг от друга не менее чем на 1.

---

### Ответ:
- **При \( a \neq 0 \):**  
  \[
  y(x) = 0 \quad \text{или} \quad y(x) = \frac{a}{x + a \, h(x)},
  \]
  где \( h(x) \) — произвольная периодическая функция с периодом \( 1 \).

- **При \( a = 0 \):**  
  \[
  y(x) \quad \text{— любая функция, такая что } y(x) \cdot y(x+1) = 0 \text{ для всех } x.
  \]

---

При подстановке \( y(x) = \dfrac{a}{x+1} \) в выражение  
\[
f = y(x)y(x+1) + a\bigl[y(x+1) - y(x)\bigr]
\]  
получаем:

\[
\begin{aligned}
y(x) &= \frac{a}{x+1}, \quad y(x+1) = \frac{a}{x+2}, \\[4pt]
y(x)y(x+1) &= \frac{a^2}{(x+1)(x+2)}, \\[4pt]
a\bigl[y(x+1) - y(x)\bigr] &= a\left( \frac{a}{x+2} - \frac{a}{x+1} \right) = a^2 \cdot \frac{(x+1) - (x+2)}{(x+1)(x+2)} = -\frac{a^2}{(x+1)(x+2)}.
\end{aligned}
\]

Суммируя:
\[
f = \frac{a^2}{(x+1)(x+2)} - \frac{a^2}{(x+1)(x+2)} = 0.
\]

Таким образом, \( y(x) = \dfrac{a}{x+1} \) действительно удовлетворяет уравнению при \( a \neq 0 \).

---

3. Решить функциональное уравнение:
$$
y(x)y(a-x)=b^2.
$$

---

Мы проверим, что функция  
\[
y(x) = b \cdot \frac{f(x)}{f(a-x)},
\]  
где \( f \) — произвольная ненулевая функция, удовлетворяет уравнению  
\[
y(x) \cdot y(a-x) = b^2.
\]

**Проверка:**  
1. Вычислим \( y(a-x) \):  
   \[
   y(a-x) = b \cdot \frac{f(a-x)}{f(a - (a-x))} = b \cdot \frac{f(a-x)}{f(x)}.
   \]

2. Перемножим \( y(x) \) и \( y(a-x) \):  
   \[
   y(x) \cdot y(a-x) = \left( b \cdot \frac{f(x)}{f(a-x)} \right) \cdot \left( b \cdot \frac{f(a-x)}{f(x)} \right) = b^2 \cdot \frac{f(x) f(a-x)}{f(a-x) f(x)} = b^2.
   \]

Таким образом, равенство выполняется для любого \( x \).

**Замечание:**  
Если \( b \neq 0 \), то эта формула описывает все ненулевые решения уравнения. В случае \( b = 0 \) решением является любая функция \( y(x) \), для которой \( y(x) \cdot y(a-x) = 0 \).

**Ответ:**  
Функция \( y(x) = b \cdot \dfrac{f(x)}{f(a-x)} \) при \( f(x) \neq 0 \) является решением исходного уравнения.

---

4. Решить функциональное уравнение:
$$
f(x+y)+f(x-y)=2f(x)+2f(y),
$$
где $f(x)$ - искомая функция.

---

Решить функциональное уравнение
$$
f(t)+g(x)Q(z)+h(x)R(z)=0,
\quad z=\varphi(x)+\psi(t),
$$
где $\varphi(x)$ и $\psi(t)$ -- произвольные функции,
$f(t)$, $g(x)$, $h(x)$, $Q(z)$, $R(z)$ -- искомые функции.

---

Решение функционального уравнения  
\[
f(t) + g(x) Q(z) + h(x) R(z) = 0, \quad z = \varphi(x) + \psi(t),
\]  
где \(\varphi(x)\) и \(\psi(t)\) — произвольные фиксированные функции, а \(f, g, h, Q, R\) — искомые достаточно гладкие функции, описывается следующим образом.

Функции должны иметь экспоненциальный вид относительно \(\varphi(x)\) и \(\psi(t)\) с возможными полиномиальными множителями в случае кратных характеристических показателей. Конкретно, существуют константы \(\lambda_1, \lambda_2\) (возможно комплексные, возможно равные) и набор констант, связанных условиями согласования, такие что

\[
\begin{aligned}
f(t) &= -K(\psi(t)), \\
g(x) &= a_1 e^{-\lambda_1 \varphi(x)} + a_2 e^{-\lambda_2 \varphi(x)}, \\
h(x) &= b_1 e^{-\lambda_1 \varphi(x)} + b_2 e^{-\lambda_2 \varphi(x)}, \\
Q(z) &= c_1 e^{\lambda_1 z} + c_2 e^{\lambda_2 z}, \\
R(z) &= d_1 e^{\lambda_1 z} + d_2 e^{\lambda_2 z},
\end{aligned}
\]

где \(K(z) = C_1 e^{\lambda_1 z} + C_2 e^{\lambda_2 z}\), а константы удовлетворяют соотношениям

\[
a_1 c_1 + b_1 d_1 = C_1, \quad a_2 c_2 + b_2 d_2 = C_2, \quad a_1 c_2 + b_1 d_2 = 0, \quad a_2 c_1 + b_2 d_1 = 0.
\]

В случае \(\lambda_1 = \lambda_2 = \lambda\) (кратный корень) выражения для функций включают полиномиальные множители, например:

\[
\begin{aligned}
K(z) &= (C_1 + C_2 z) e^{\lambda z}, \\
g(x) &= (a_1 + a_2 \varphi(x)) e^{-\lambda \varphi(x)}, \\
h(x) &= (b_1 + b_2 \varphi(x)) e^{-\lambda \varphi(x)}, \\
Q(z) &= (c_1 + c_2 z) e^{\lambda z}, \\
R(z) &= (d_1 + d_2 z) e^{\lambda z},
\end{aligned}
\]

с аналогичными условиями на константы, обеспечивающими выполнение уравнения.

Все остальные решения получаются из указанных линейными преобразованиями, сохраняющими форму суммы произведений.

\[
\boxed{
\begin{aligned}
&f(t) = -K(\psi(t)), \quad K(z) = C_1 e^{\lambda_1 z} + C_2 e^{\lambda_2 z}, \\
&g(x) = a_1 e^{-\lambda_1 \varphi(x)} + a_2 e^{-\lambda_2 \varphi(x)}, \\
&h(x) = b_1 e^{-\lambda_1 \varphi(x)} + b_2 e^{-\lambda_2 \varphi(x)}, \\
&Q(z) = c_1 e^{\lambda_1 z} + c_2 e^{\lambda_2 z}, \\
&R(z) = d_1 e^{\lambda_1 z} + d_2 e^{\lambda_2 z},
\end{aligned}
}
\]  
где константы связаны соотношениями \(a_1 c_1 + b_1 d_1 = C_1\), \(a_2 c_2 + b_2 d_2 = C_2\), \(a_1 c_2 + b_1 d_2 = 0\), \(a_2 c_1 + b_2 d_1 = 0\).

---

Чему равны $\lambda_1$ и $\lambda_2$?

---

В уравнении  
\[
f(t) + g(x) Q(z) + h(x) R(z) = 0, \quad z = \varphi(x) + \psi(t),
\]  
параметры \(\lambda_1, \lambda_2\) (и другие возможные \(\lambda_i\)) являются **произвольными константами** (возможно комплексными), которые возникают как показатели экспонент в общем решении. Они не фиксируются самим уравнением, а служат свободными параметрами, определяющими конкретное решение. В случае двух различных показателей \(\lambda_1\) и \(\lambda_2\) функции имеют вид экспонент \(e^{\lambda_i z}\), а в случае кратного корня появляются полиномиальные множители. Эти параметры отражают линейную независимость базисных решений линейного ОДУ второго порядка, которому удовлетворяют \(Q(z)\) и \(R(z)\).