1. Решить функциональное уравнение:
$$
y(x)y(x+1)+a[y(x+1)-y(x)]=0,
$$
где $a$ - свободный параметр.

---

### Решение функционального уравнения
\[
y(x) \, y(x+1) + a \, \bigl[ y(x+1) - y(x) \bigr] = 0,
\]
где \( a \) — свободный параметр.

---

### 1. Случай \( a \neq 0 \)

Уравнение преобразуется к виду:
\[
y(x+1) \bigl( y(x) + a \bigr) = a \, y(x).
\]
Если \( y(x) + a \neq 0 \), то
\[
y(x+1) = \frac{a \, y(x)}{y(x) + a}.
\]

#### 1.1. Нетривиальные решения (\( y(x) \not\equiv 0 \))
С помощью замены \( z(x) = \dfrac{1}{y(x)} \) получаем:
\[
z(x+1) = z(x) + \frac{1}{a}.
\]
Общее решение этого разностного уравнения:
\[
z(x) = \frac{x}{a} + h(x),
\]
где \( h(x) \) — произвольная периодическая функция с периодом \( 1 \), то есть \( h(x+1) = h(x) \). Тогда
\[
y(x) = \frac{1}{z(x)} = \frac{a}{x + a \, h(x)}.
\]

**Замечание:** При таком построении автоматически выполняется \( y(x) \neq 0 \) и \( y(x) \neq -a \), что согласуется с предположениями.

#### 1.2. Тривиальное решение
Непосредственной подстановкой проверяется, что \( y(x) \equiv 0 \) также является решением при любом \( a \).

---

### 2. Случай \( a = 0 \)

Уравнение принимает вид:
\[
y(x) \, y(x+1) = 0 \quad \text{для всех } x.
\]
Это означает, что для каждого \( x \) хотя бы одно из значений \( y(x) \) или \( y(x+1) \) равно нулю. Общее решение — произвольная функция \( y(x) \), удовлетворяющая условию:
\[
y(x) \cdot y(x+1) = 0 \quad \forall x.
\]
Например, функция может быть нулевой на всей оси или принимать ненулевые значения только на непересекающихся интервалах длины не более 1, отстоящих друг от друга не менее чем на 1.

---

### Ответ:
- **При \( a \neq 0 \):**  
  \[
  y(x) = 0 \quad \text{или} \quad y(x) = \frac{a}{x + a \, h(x)},
  \]
  где \( h(x) \) — произвольная периодическая функция с периодом \( 1 \).

- **При \( a = 0 \):**  
  \[
  y(x) \quad \text{— любая функция, такая что } y(x) \cdot y(x+1) = 0 \text{ для всех } x.
  \]

---

Подставить в правую часть выражения $f=y(x)y(x+1)+a[y(x+1)-y(x)]$ функцию $y(x)=\frac a{x+1}$.

---

При подстановке \( y(x) = \dfrac{a}{x+1} \) в выражение  
\[
f = y(x)y(x+1) + a\bigl[y(x+1) - y(x)\bigr]
\]  
получаем:

\[
\begin{aligned}
y(x) &= \frac{a}{x+1}, \quad y(x+1) = \frac{a}{x+2}, \\[4pt]
y(x)y(x+1) &= \frac{a^2}{(x+1)(x+2)}, \\[4pt]
a\bigl[y(x+1) - y(x)\bigr] &= a\left( \frac{a}{x+2} - \frac{a}{x+1} \right) = a^2 \cdot \frac{(x+1) - (x+2)}{(x+1)(x+2)} = -\frac{a^2}{(x+1)(x+2)}.
\end{aligned}
\]

Суммируя:
\[
f = \frac{a^2}{(x+1)(x+2)} - \frac{a^2}{(x+1)(x+2)} = 0.
\]

Таким образом, \( y(x) = \dfrac{a}{x+1} \) действительно удовлетворяет уравнению при \( a \neq 0 \).

---

2. Решить функциональное уравнение:
$$
y(\sin x)-y(\cos x)=0,
$$
где $y=y(z)$ - искомая функция.

---

Решим функциональное уравнение:

\[
y(\sin x) - y(\cos x) = 0,
\]

где \( y = y(z) \) — искомая функция.

### 1. Преобразование условия
Уравнение означает, что для всех \( x \) выполнено \( y(\sin x) = y(\cos x) \). Так как \( \sin^2 x + \cos^2 x = 1 \), это условие эквивалентно тому, что \( y(a) = y(b) \) для любых \( a, b \in [-1, 1] \), удовлетворяющих \( a^2 + b^2 = 1 \).

### 2. Свойства функции \( y \)
- Из условия следует, что \( y \) чётная на \( [-1, 1] \): для любого \( t \in [0, 1] \) возьмём \( x = \arcsin t \), тогда \( \cos x = \sqrt{1 - t^2} \), и также можно получить \( \cos x = -\sqrt{1 - t^2} \), откуда \( y(t) = y(-t) \).
- Для \( u \in [0, 1] \) имеем \( y(u) = y(\sqrt{1 - u^2}) \). Это соотношение инволютивно: \( y(\sqrt{1 - u^2}) = y(u) \).

### 3. Общее решение
Рассмотрим отображение \( \phi(u) = \sqrt{1 - u^2} \) на отрезке \( [0, 1] \). Оно разбивает \( [0, 1] \) на пары вида \( \{ u, \phi(u) \} \), за исключением неподвижной точки \( u_0 = \frac{1}{\sqrt{2}} \), где \( u_0 = \phi(u_0) \).

Функция \( y \) должна принимать одинаковые значения на каждой такой паре, а в точке \( u_0 \) может быть произвольной. Удобно параметризовать решение, введя функцию \( f \), определённую на \( [0, 1] \), такую что \( f(t) = f(1 - t) \). Тогда общее решение имеет вид:

\[
y(z) = f(z^2), \quad z \in [-1, 1],
\]

где \( f \) — произвольная функция, удовлетворяющая условию \( f(t) = f(1 - t) \) для всех \( t \in [0, 1] \).

### 4. Проверка
Если \( y(z) = f(z^2) \) и \( f(t) = f(1 - t) \), то
\[
y(\sin x) = f(\sin^2 x), \quad y(\cos x) = f(\cos^2 x) = f(1 - \sin^2 x) = f(\sin^2 x),
\]
следовательно, условие выполнено.

Обратно, если \( y \) удовлетворяет исходному уравнению, то, полагая \( f(t) = y(\sqrt{t}) \) для \( t \in [0, 1] \), получаем \( f(t) = f(1 - t) \) и \( y(z) = f(z^2) \) (с учётом чётности).

### Ответ:
\[
\boxed{y(z) = f(z^2), \quad \text{где } f \text{ — любая функция, такая что } f(t) = f(1 - t) \text{ для } t \in [0, 1].}
\]

---

3. Решить функциональное уравнение:
$$
y(x)y(a-x)=b^2.
$$

---

### Решение функционального уравнения

\[
y(x) \, y(a - x) = b^2,
\]
где \( a \) и \( b \) — константы.

---

### 1. Случай \( b \neq 0 \)

Из уравнения следует, что \( y(x) \neq 0 \) для всех \( x \), так как в противном случае левая часть обратилась бы в ноль, а правая равна \( b^2 \neq 0 \).

Поделим обе части на \( b^2 \) и обозначим \( u(x) = \dfrac{y(x)}{b} \). Тогда уравнение примет вид:
\[
u(x) \, u(a - x) = 1.
\]

**Общее решение:**
\[
u(x) = \frac{f(x)}{f(a - x)},
\]
где \( f(x) \) — произвольная функция, не обращающаяся в ноль. Действительно, подстановка даёт:
\[
u(a - x) = \frac{f(a - x)}{f(x)}, \quad \text{и } u(x) u(a - x) = 1.
\]

Возвращаясь к \( y(x) \), получаем:
\[
\boxed{y(x) = b \cdot \frac{f(x)}{f(a - x)}}, \quad f(x) \neq 0 \text{ для всех } x.
\]

Эквивалентная форма через экспоненту:  
Пусть \( y(x) = b \, e^{g(x)} \), тогда \( g(x) + g(a - x) = 0 \), то есть \( g \) антисимметрична относительно \( a/2 \). Если ввести \( h(t) = g(t + a/2) \), то \( h \) — нечётная функция. Таким образом,
\[
y(x) = b \, e^{h(x - a/2)}, \quad h \text{ — нечётная}.
\]

---

### 2. Случай \( b = 0 \)

Уравнение превращается в:
\[
y(x) \, y(a - x) = 0 \quad \text{для всех } x.
\]

Это означает, что для каждого \( x \) хотя бы одно из значений \( y(x) \) или \( y(a - x) \) равно нулю. Общее решение можно описать следующим образом:  
Выберем произвольное множество \( S \subset \mathbb{R} \) такое, что если \( x \in S \), то \( a - x \notin S \) (т.е. \( S \) не содержит пар симметричных относительно \( a/2 \)). Тогда положим:
- \( y(x) \) — произвольная (возможно, ненулевая) функция при \( x \in S \),
- \( y(x) = 0 \) при \( x \notin S \).

**Ответ при \( b = 0 \):**
\[
\boxed{\text{Функция } y(x) \text{ произвольна, но для каждого } x \text{ выполняется } y(x) \cdot y(a - x) = 0.}
\]

---

### Итоговый ответ:
- **Если \( b \neq 0 \):**  
  \[
  y(x) = b \cdot \frac{f(x)}{f(a - x)}, \quad f(x) \neq 0 \; \forall x,
  \]
  или в эквивалентной форме \( y(x) = b \, e^{h(x - a/2)} \), где \( h \) — нечётная функция.
- **Если \( b = 0 \):**  
  \[
  y(x) \text{ — любая функция, удовлетворяющая условию } y(x) \cdot y(a - x) = 0 \; \forall x.
  \]

---

Показать, что решением этого уравнения является также функция $$
y(x)=b\frac{f(x)}{f(a-x)}. 
$$

---

Мы проверим, что функция  
\[
y(x) = b \cdot \frac{f(x)}{f(a-x)},
\]  
где \( f \) — произвольная ненулевая функция, удовлетворяет уравнению  
\[
y(x) \cdot y(a-x) = b^2.
\]

**Проверка:**  
1. Вычислим \( y(a-x) \):  
   \[
   y(a-x) = b \cdot \frac{f(a-x)}{f(a - (a-x))} = b \cdot \frac{f(a-x)}{f(x)}.
   \]

2. Перемножим \( y(x) \) и \( y(a-x) \):  
   \[
   y(x) \cdot y(a-x) = \left( b \cdot \frac{f(x)}{f(a-x)} \right) \cdot \left( b \cdot \frac{f(a-x)}{f(x)} \right) = b^2 \cdot \frac{f(x) f(a-x)}{f(a-x) f(x)} = b^2.
   \]

Таким образом, равенство выполняется для любого \( x \).

**Замечание:**  
Если \( b \neq 0 \), то эта формула описывает все ненулевые решения уравнения. В случае \( b = 0 \) решением является любая функция \( y(x) \), для которой \( y(x) \cdot y(a-x) = 0 \).

**Ответ:**  
Функция \( y(x) = b \cdot \dfrac{f(x)}{f(a-x)} \) при \( f(x) \neq 0 \) является решением исходного уравнения.

---

Показать, что решением этого уравнения является также функция $$
y(x)=-b\frac{f(x)}{f(a-x)}. 
$$

---

Подстановка функции  
\[
y(x) = -b \cdot \frac{f(x)}{f(a-x)}, \quad f(x) \neq 0,
\]  
в уравнение \( y(x) \cdot y(a-x) = b^2 \) даёт:

\[
\begin{aligned}
y(a-x) &= -b \cdot \frac{f(a-x)}{f(a - (a-x))} = -b \cdot \frac{f(a-x)}{f(x)}, \\[4pt]
y(x) \cdot y(a-x) &= \left( -b \cdot \frac{f(x)}{f(a-x)} \right) \cdot \left( -b \cdot \frac{f(a-x)}{f(x)} \right) = b^2 \cdot \frac{f(x) f(a-x)}{f(a-x) f(x)} = b^2.
\end{aligned}
\]

Таким образом, функция удовлетворяет уравнению.

**Замечание:**  
При \( b \neq 0 \) общее решение можно записать в виде  
\[
y(x) = b \cdot \frac{f(x)}{f(a-x)},
\]  
так как замена \( f(x) \to -f(x) \) не меняет выражения. Поэтому форма с дополнительным знаком «минус» не даёт новых решений, но подтверждает корректность общего вида.

---

На самом деле есть два решения, которые отличаются только знаком.

---

Уравнение имеет вид \( y(x) \, y(a - x) = b^2 \).

### Проверка для функции \( y(x) = -b \cdot \frac{f(x)}{f(a - x)} \):
Вычислим:
\[
y(a - x) = -b \cdot \frac{f(a - x)}{f(a - (a - x))} = -b \cdot \frac{f(a - x)}{f(x)}.
\]
Тогда:
\[
y(x) \cdot y(a - x) = \left(-b \cdot \frac{f(x)}{f(a - x)}\right) \cdot \left(-b \cdot \frac{f(a - x)}{f(x)}\right) = b^2 \cdot \frac{f(x) f(a - x)}{f(a - x) f(x)} = b^2.
\]

Таким образом, функция \( y(x) = -b \cdot \frac{f(x)}{f(a - x)} \) удовлетворяет уравнению.

**Замечание:** Общее решение при \( b \neq 0 \) можно записать в виде:
\[
y(x) = b \cdot \frac{f(x)}{f(a - x)},
\]
где \( f \) — произвольная ненулевая функция, так как знак «минус» можно включить в \( f \) (например, заменив \( f(x) \) на \( -f(x) \)). Тем не менее, явная проверка показывает, что оба выражения являются решениями.

---

4. Решить функциональное уравнение:
$$
f(x+y)+f(x-y)=2f(x)+2f(y),
$$
где $f(x)$ - искомая функция.

---

Функциональное уравнение  
\[
f(x+y) + f(x-y) = 2f(x) + 2f(y)
\]  
имеет решения в классе непрерывных функций вида  
\[
f(x) = C x^2, \quad C \in \mathbb{R}.
\]  
Это проверяется непосредственной подстановкой:  
\[
C(x+y)^2 + C(x-y)^2 = C(2x^2 + 2y^2) = 2Cx^2 + 2Cy^2 = 2f(x) + 2f(y).
\]  
Кроме того, \( f(0) = 0 \) и функция является чётной.

Если не предполагать непрерывность, то существуют и другие решения, конструируемые с помощью базиса Гамеля над \(\mathbb{Q}\): для произвольного базиса \(\{e_i\}\) действительных чисел как \(\mathbb{Q}\)-векторного пространства и произвольных значений \(f(e_i)\) функция продолжается на все числа с сохранением свойства \(f(qx) = q^2 f(x)\) для рациональных \(q\) и аддитивности квадратичной формы. Однако в стандартной постановке задачи обычно предполагается непрерывность или ищутся простейшие решения, поэтому ответ \(f(x) = Cx^2\) является основным.

\[
\boxed{f(x) = C x^{2}, \quad C \in \mathbb{R}}
\]

---

5. Решить функциональное уравнение:
$$
f(x,y)=a^nf(x+(1-a)y,ay),
$$
где $a>0$ - свободный параметр, $n$ - заданное число, $f(x,y)$ - искомая функция.

---

### Решение функционального уравнения
\[
f(x, y) = a^n f\!\left(x + (1 - a)y,\ a y\right), \quad a > 0,
\]
где \( n \) — заданное число, \( f(x, y) \) — искомая функция.

---

#### 1. Преобразование уравнения  
Введём замену:
\[
u = x + y, \quad v = y,
\]
тогда \( x = u - v \), и пусть \( g(u, v) = f(u - v, v) \). Тогда
\[
f(x + (1 - a)y,\ a y) = g(u, a v).
\]
Исходное уравнение переписывается в виде:
\[
g(u, v) = a^n g(u, a v) \quad \forall u, v \in \mathbb{R}, \ \forall a > 0.
\]

---

#### 2. Решение для фиксированного \( u \)  
При фиксированном \( u \) обозначим \( h(v) = g(u, v) \). Тогда
\[
h(v) = a^n h(a v) \quad \forall a > 0, \ \forall v.
\]

- **Случай \( v \neq 0 \):**  
  Для \( v > 0 \) положим \( t = \ln v \), \( H(t) = h(e^t) \). Тогда уравнение становится:
  \[
  H(t) = a^n H(t + \ln a).
  \]
  Полагая \( b = \ln a \), получаем:
  \[
  H(t + b) = e^{-bn} H(t) \quad \forall b \in \mathbb{R}.
  \]
  Фиксируя \( t = 0 \), находим:
  \[
  H(b) = H(0) e^{-bn}.
  \]
  Возвращаясь к \( h \):
  \[
  h(v) = H(\ln v) = H(0) v^{-n} \quad (v > 0).
  \]
  Аналогично для \( v < 0 \), полагая \( w = -v > 0 \), получаем:
  \[
  h(v) = \tilde{h}(w) = \tilde{h}(0) w^{-n} = C |v|^{-n},
  \]
  где постоянные \( H(0) \) и \( \tilde{h}(0) \) могут различаться для разных знаков \( v \).  
  Таким образом, для \( v \neq 0 \):
  \[
  h(v) = \Phi(\operatorname{sgn}(v)) |v|^{-n},
  \]
  где \( \Phi \) зависит от \( u \) и знака \( v \).

- **Случай \( v = 0 \):**  
  Уравнение даёт:
  \[
  h(0) = a^n h(0) \quad \forall a > 0.
  \]
  При \( n \neq 0 \) выбираем \( a \neq 1 \) такое, что \( a^n \neq 1 \), тогда \( h(0) = 0 \).  
  При \( n = 0 \) условие выполняется тождественно, и \( h(0) \) произвольно.

---

#### 3. Возврат к \( f(x, y) \)  
Так как \( g(u, v) = f(u - v, v) \) и \( u = x + y \), \( v = y \), то
\[
f(x, y) = g(x + y, y).
\]

- **При \( y \neq 0 \):**  
  \[
  f(x, y) = \varphi(x + y, \operatorname{sgn}(y)) \cdot |y|^{-n},
  \]
  где \( \varphi \) — произвольная функция двух аргументов (первый — вещественный, второй — знак \( y \)).
- **При \( y = 0 \):**  
  - Если \( n \neq 0 \), то \( f(x, 0) = 0 \).
  - Если \( n = 0 \), то \( f(x, 0) \) — произвольная функция от \( x \).

---

#### 4. Компактная запись  
Общее решение можно записать в виде:
\[
f(x, y) = 
\begin{cases}
\Phi(x + y) \cdot |y|^{-n}, & y \neq 0, \\
0, & y = 0, \ n \neq 0,
\end{cases}
\]
где \( \Phi \) — произвольная функция, допускающая различное определение для \( y > 0 \) и \( y < 0 \).  
При \( n = 0 \) решение упрощается:
\[
f(x, y) = \Phi(x + y) \quad \text{для всех } x, y.
\]

---

**Ответ:**
\[
\boxed{f(x,y) = \Phi(x+y) \cdot |y|^{-n} \ \text{при } y \neq 0, \quad f(x,0) = 0 \ \text{при } n \neq 0; \ \text{при } n=0: f(x,y) = \Phi(x+y).}
\]

---

Решить функциональное уравнение:
$$
f(x+y)=f(x)+f(y)-af(x)f(y),
$$
где $a\not=0$ - свободный параметр, $f(x)$ - искомая функция.

---

Функциональное уравнение:

\[
f(x+y) = f(x) + f(y) - a f(x) f(y), \quad a \neq 0.
\]

С помощью замены \( g(x) = 1 - a f(x) \) уравнение сводится к мультипликативной форме:

\[
g(x+y) = g(x) g(y).
\]

Общее решение для \( g \) — либо \( g \equiv 0 \), либо \( g(x) = e^{A(x)} \), где \( A(x) \) — аддитивная функция (\( A(x+y) = A(x) + A(y) \)). Возвращаясь к \( f \):

\[
f(x) = \frac{1 - g(x)}{a}.
\]

Таким образом, общее решение:

\[
\boxed{f(x) = \frac{1 - E(x)}{a}, \quad \text{где } E(x) \text{ удовлетворяет } E(x+y)=E(x)E(y) \text{ для всех } x, y.}
\]

Частные случаи:

- При \( E(x) \equiv 0 \): \( f(x) \equiv \dfrac{1}{a} \).
- При \( E(x) \equiv 1 \): \( f(x) \equiv 0 \).
- Если \( E(x) = e^{cx} \), то \( f(x) = \dfrac{1 - e^{cx}}{a} \).
- В классе непрерывных функций: либо \( f \equiv 0 \), либо \( f(x) = \dfrac{1 - e^{cx}}{a} \) для некоторой константы \( c \in \mathbb{R} \).

---

Решить функциональное уравнение:
$$
\biggl(\frac{f^2(x)+f^2(y)}2\biggr)^{1/2}=f\biggl(\biggl(\frac{x^2+y^2}2\biggr)^{1/2}\biggr),
$$
где $f(x)$ - искомая функция.

---

Функциональное уравнение  
\[
\sqrt{\frac{f^2(x)+f^2(y)}{2}} = f\left(\sqrt{\frac{x^2+y^2}{2}}\right)
\]  
сводится к уравнению Йенсена для функции \( h(u) = f(\sqrt{u})^2 \), откуда при условии непрерывности (или измеримости) получаем \( h(u) = \alpha u + \beta \). Учитывая неотрицательность левой части, находим общее решение:  
\[
f(x) = \sqrt{\alpha x^2 + \beta}, \quad \alpha, \beta \ge 0.
\]

\boxed{f(x) = \sqrt{\alpha x^{2} + \beta}, \quad \alpha, \beta \ge 0.}

---

Решить функциональное уравнение
$$
f(x+y)=g(x)+f(y)-f(x)h(y),
$$
где $f(x)$, $g(x)$, $h(y)$ - искомые достаточно гладкие функции.

---

Функциональное уравнение  
\[
f(x+y) = g(x) + f(y) - f(x)h(y),
\]  
где \( f, g, h \) — достаточно гладкие функции, имеет два семейства решений.

### 1. Случай \( f \not\equiv 0 \)  
Функции имеют вид:  
\[
f(x) = \lambda x + a, \quad g(x) = \lambda(1+c)x + a c, \quad h(y) = c,
\]  
где \( \lambda, a, c \) — произвольные действительные константы.

### 2. Случай \( f \equiv 0 \)  
Тогда:  
\[
f(x) \equiv 0, \quad g(x) \equiv 0, \quad h(y) \text{ — произвольная достаточно гладкая функция}.
\]

### Проверка  
- Для первого семейства подстановка даёт тождество.  
- Для второго семейства уравнение сводится к \( 0 = 0 \).

Таким образом, получены все решения в классе достаточно гладких функций.

\[
\boxed{
\begin{cases}
f(x)=\lambda x+a,\quad g(x)=\lambda(1+c)x+ac,\quad h(y)=c, \quad \lambda,a,c\in\mathbb{R};\\
f(x)\equiv 0,\quad g(x)\equiv 0,\quad h(y) \text{ — произвольная достаточно гладкая функция}.
\end{cases}
}
\]

---

Некоторые решения потеряны. Проверьте это положив $f(y)=g(y)$ и $h(y)=af(y)$, где $a$ - свободный параметр (такое уравнение мы рассматривали ранее).

---

Все достаточно гладкие решения функционального уравнения  
\[
f(x+y) = g(x) + f(y) - f(x)h(y)
\]  
исчерпываются следующими случаями:

1. **Тривиальное решение:**  
   \[
   f(x) \equiv 0, \quad g(x) \equiv 0, \quad h(y) \text{ — произвольная гладкая функция}.
   \]

2. **Линейное семейство:**  
   Существуют константы \(\alpha, b, c \in \mathbb{R}\) такие, что  
   \[
   f(x) = \alpha x + b, \quad h(y) \equiv c, \quad g(x) = (1+c)(\alpha x + b) - b.
   \]  
   (При \(\alpha = 0\) функция \(f\) постоянна.)

3. **Экспоненциальное семейство:**  
   Существуют константы \(\beta, k, c \in \mathbb{R}\), \(k \neq 0\), такие, что  
   \[
   f(x) = \frac{e^{\beta x} - 1}{k}, \quad h(y) = (c+1) - e^{\beta y}, \quad g(x) = (1+c)f(x),
   \]  
   при этом \(f(0) = 0\) (т.е. \(b = 0\)).  

Частный случай \(f = g\) и \(h(y) = a f(y)\) соответствует экспоненциальному семейству при \(c = 0\), \(k = -a\), и приводит к уравнению \(f(x+y) = f(x) + f(y) - a f(x) f(y)\), решением которого (при \(a \neq 0\)) является \(f(x) = \frac{1 - e^{\beta x}}{a}\).

---

Решить функциональное уравнение
$$
f(t)+g(x)Q(z)+h(x)R(z)=0,
\quad z=\varphi(x)+\psi(t),
$$
где $\varphi(x)$ и $\psi(t)$ -- произвольные функции,
$f(t)$, $g(x)$, $h(x)$, $Q(z)$, $R(z)$ -- искомые функции.

---

Решение функционального уравнения  
\[
f(t) + g(x) Q(z) + h(x) R(z) = 0, \quad z = \varphi(x) + \psi(t),
\]  
где \(\varphi(x)\) и \(\psi(t)\) — произвольные фиксированные функции, а \(f, g, h, Q, R\) — искомые достаточно гладкие функции, описывается следующим образом.

Функции должны иметь экспоненциальный вид относительно \(\varphi(x)\) и \(\psi(t)\) с возможными полиномиальными множителями в случае кратных характеристических показателей. Конкретно, существуют константы \(\lambda_1, \lambda_2\) (возможно комплексные, возможно равные) и набор констант, связанных условиями согласования, такие что

\[
\begin{aligned}
f(t) &= -K(\psi(t)), \\
g(x) &= a_1 e^{-\lambda_1 \varphi(x)} + a_2 e^{-\lambda_2 \varphi(x)}, \\
h(x) &= b_1 e^{-\lambda_1 \varphi(x)} + b_2 e^{-\lambda_2 \varphi(x)}, \\
Q(z) &= c_1 e^{\lambda_1 z} + c_2 e^{\lambda_2 z}, \\
R(z) &= d_1 e^{\lambda_1 z} + d_2 e^{\lambda_2 z},
\end{aligned}
\]

где \(K(z) = C_1 e^{\lambda_1 z} + C_2 e^{\lambda_2 z}\), а константы удовлетворяют соотношениям

\[
a_1 c_1 + b_1 d_1 = C_1, \quad a_2 c_2 + b_2 d_2 = C_2, \quad a_1 c_2 + b_1 d_2 = 0, \quad a_2 c_1 + b_2 d_1 = 0.
\]

В случае \(\lambda_1 = \lambda_2 = \lambda\) (кратный корень) выражения для функций включают полиномиальные множители, например:

\[
\begin{aligned}
K(z) &= (C_1 + C_2 z) e^{\lambda z}, \\
g(x) &= (a_1 + a_2 \varphi(x)) e^{-\lambda \varphi(x)}, \\
h(x) &= (b_1 + b_2 \varphi(x)) e^{-\lambda \varphi(x)}, \\
Q(z) &= (c_1 + c_2 z) e^{\lambda z}, \\
R(z) &= (d_1 + d_2 z) e^{\lambda z},
\end{aligned}
\]

с аналогичными условиями на константы, обеспечивающими выполнение уравнения.

Все остальные решения получаются из указанных линейными преобразованиями, сохраняющими форму суммы произведений.

\[
\boxed{
\begin{aligned}
&f(t) = -K(\psi(t)), \quad K(z) = C_1 e^{\lambda_1 z} + C_2 e^{\lambda_2 z}, \\
&g(x) = a_1 e^{-\lambda_1 \varphi(x)} + a_2 e^{-\lambda_2 \varphi(x)}, \\
&h(x) = b_1 e^{-\lambda_1 \varphi(x)} + b_2 e^{-\lambda_2 \varphi(x)}, \\
&Q(z) = c_1 e^{\lambda_1 z} + c_2 e^{\lambda_2 z}, \\
&R(z) = d_1 e^{\lambda_1 z} + d_2 e^{\lambda_2 z},
\end{aligned}
}
\]  
где константы связаны соотношениями \(a_1 c_1 + b_1 d_1 = C_1\), \(a_2 c_2 + b_2 d_2 = C_2\), \(a_1 c_2 + b_1 d_2 = 0\), \(a_2 c_1 + b_2 d_1 = 0\).

---

Чему равны $\lambda_1$ и $\lambda_2$?

---

В уравнении  
\[
f(t) + g(x) Q(z) + h(x) R(z) = 0, \quad z = \varphi(x) + \psi(t),
\]  
параметры \(\lambda_1, \lambda_2\) (и другие возможные \(\lambda_i\)) являются **произвольными константами** (возможно комплексными), которые возникают как показатели экспонент в общем решении. Они не фиксируются самим уравнением, а служат свободными параметрами, определяющими конкретное решение. В случае двух различных показателей \(\lambda_1\) и \(\lambda_2\) функции имеют вид экспонент \(e^{\lambda_i z}\), а в случае кратного корня появляются полиномиальные множители. Эти параметры отражают линейную независимость базисных решений линейного ОДУ второго порядка, которому удовлетворяют \(Q(z)\) и \(R(z)\).