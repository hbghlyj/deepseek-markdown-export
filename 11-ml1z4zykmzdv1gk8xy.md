Задача. Рассмотрим уравнение 
\begin{equation}
u_{t}=u_{xx}+F(u,u_x).
\end{equation}
Требуется найти наиболее общую функцию $F(u,w)$, для которой это уравнение допускает автомодельное решение.

---

Уравнение \( u_t = u_{xx} + F(u, u_x) \) допускает автомодельное решение вида \( u(x,t) = t^b \phi(x/\sqrt{t}) \) тогда и только тогда, когда функция \( F(u, w) \) удовлетворяет условию инвариантности относительно растяжения переменных. Это условие приводит к двум возможным формам \( F \):

1. **Случай \( b \neq 0 \):**  
   \[
   F(u, w) = u^{p} \, \Phi\!\left( u^{q} w \right),
   \]
   где \( p \) и \( q \) — константы, связанные соотношением \( p + 2q = -1 \), а \( \Phi \) — произвольная функция одного аргумента.

2. **Случай \( b = 0 \):**  
   \[
   F(u, w) = w^{2} \, \Psi(u),
   \]
   где \( \Psi \) — произвольная функция.

Эти два семейства исчерпывают все возможности для существования автомодельных решений в данном уравнении.

---

Найти точные решения уравнения
\begin{equation}
u_{xy}^2-u_{xx}u_{yy}=f(x)u^k,
\end{equation}
где $f(x)$ --- произвольная функция, $k$ --- свободный параметр,
которые выражаются в квадратурах или через решения обыкновенного дифференциального уравнения.

---

Уравнение \( u_{xy}^2 - u_{xx} u_{yy} = f(x) u^k \) допускает следующие семейства точных решений, выражающихся в квадратурах или через решения обыкновенных дифференциальных уравнений:

### 1. Случай \( k \neq 2 \)
#### а) Решение с линейной функцией \( g(x) \):
\[
u(x, y) = \phi(x) \bigl( y + a x + b \bigr)^{\frac{2}{2-k}},
\]
где \( a, b \) — произвольные постоянные, а функция \( \phi(x) \) удовлетворяет ОДУ:
\[
4 \phi'^2 - 2k \phi \phi'' = (2-k)^2 f(x) \phi^k.
\]

#### б) Решение со степенной функцией \( \phi(x) \):
\[
u(x, y) = A (x + x_0)^{-\frac{1+k}{2-k}} \bigl( y + g(x) \bigr)^{\frac{3}{2-k}},
\]
где \( A, x_0 \) — произвольные постоянные, а функция \( g(x) \) определяется из условия:
\[
g''(x) = -\frac{(2-k)^3}{9(1+k)} A^{k-2} f(x) (x + x_0)^{1+k},
\]
и находится двукратным интегрированием (выражается в квадратурах).

### 2. Случай \( k = 2 \)
\[
u(x, y) = D \exp\left( \beta y - \frac{1}{\beta^2} \int f(x) \,dx - C x \right),
\]
где \( \beta \neq 0 \), \( C, D \) — произвольные постоянные.

Эти семейства исчерпывают все решения указанного типа для произвольной функции \( f(x) \) и параметра \( k \).

---

Проверьте, пожалуйста, последнее решение прямой подстановкой в исходное уравнение.

---



---

Уточняю, второе решение при произвольном $k$ (решение со степенной функцией).

---

Уравнение
\[
u_{xy}^2-u_{xx}u_{yy}=f(x)u^k
\]
допускает следующее семейство точных решений при \( k\neq 2,\; k\neq -1 \):

\[
u(x,y)=A\,(x+x_0)^{-\frac{1+k}{2-k}}\,\bigl(y+g(x)\bigr)^{\frac{3}{2-k}},
\]
где \(A\) и \(x_0\) — произвольные постоянные, а функция \(g(x)\) определяется из обыкновенного дифференциального уравнения
\[
g''(x)=-\frac{(2-k)^3}{9(1+k)}\,A^{\,k-2}\,f(x)\,(x+x_0)^{\,1+k},
\]
и находится двукратным интегрированием (выражается в квадратурах).

### Вывод и пояснения:
1. **Анзац:** решение ищется в виде  
   \[
   u(x,y)=\varphi(x)\,h\bigl(y+g(x)\bigr),
   \]  
   где \(h(\eta)=\eta^\alpha\).

2. **Определение параметров:** подстановка анзаца в уравнение приводит к условию  
   \[
   \varphi'^2\alpha-\varphi\varphi''(\alpha-1)=0,
   \]  
   которое при степенном выборе \(\varphi(x)=A(x+x_0)^\beta\) даёт соотношение \(\beta=1-\alpha\).

3. **Согласование степеней:** требование одинаковой степени по \(\eta\) в оставшихся слагаемых фиксирует  
   \[
   \alpha=\frac{3}{2-k},\qquad \beta=-\frac{1+k}{2-k}.
   \]

4. **Уравнение для \(g\):** после подстановки \(\varphi\) и \(\alpha\) получается приведённое выше ОДУ для \(g''(x)\).

### Особые случаи:
- При \(k=2\) решение имеет экспоненциальный вид (см. первоначальный ответ).
- При \(k=-1\) параметр \(\alpha=1\), что обращает знаменатель в выражении для \(g''\) в ноль; этот случай требует отдельного рассмотрения.
- При \(k=2\) или \(k=-1\) данная формула неприменима, но другие семейства решений (первое или экспоненциальное) остаются в силе.

---

Попробуйте искать еще решение в виде $u=\varphi(x)y^{\frac{k+2}2}$.

---

Решение в виде \( u = \varphi(x) \, y^{(k+2)/2} \) возможно только при \( k = 0 \), что приводит к линейному по \( y \) решению:

\[
u(x, y) = a(x) + y \, b(x),
\]
где \( b(x) = \pm \int \sqrt{f(x)} \, dx + C \), а \( a(x) \) — произвольная функция.

Для произвольного \( k \neq 2, -1 \) более общий вид \( u = \varphi(x) \, y^{2/(2-k)} \) является частным случаем первого семейства решений (при \( g(x) = 0 \)) и приводит к ОДУ для \( \varphi(x) \):

\[
4 (\varphi')^2 - 2k \varphi \varphi'' = (2-k)^2 f(x) \varphi^k.
\]

Таким образом, предложенный анзац не даёт новых решений по сравнению с уже известными семействами.

---

Найти точные решения уравнения
\begin{equation}
u_{xy}^2-u_{xx}u_{yy}=f(x)y^k,
\label{e50}
\end{equation}
где $f(x)$ --- произвольная функция, $k$ --- свободный параметр,
которые выражаются в квадратурах или через решения обыкновенного дифференциального уравнения.

---

Для уравнения \( u_{xy}^2 - u_{xx}u_{yy} = f(x) y^k \) получены следующие точные решения, выражаемые в квадратурах или через решения обыкновенных дифференциальных уравнений:

### 1. Случай \( k \neq -2 \)
\[
u(x,y) = \varphi(x) \, y^{\frac{k+2}{2}} + c_1 x + c_2,
\]
где \( c_1, c_2 \) — произвольные постоянные, а функция \( \varphi(x) \) удовлетворяет ОДУ
\[
(k+2)^2 \bigl(\varphi'(x)\bigr)^2 - k(k+2) \, \varphi(x) \, \varphi''(x) = 4 f(x).
\]

### 2. Случай \( k = -2 \)
\[
u(x,y) = (A x + B) \ln y + \psi(x),
\]
где \( A, B \) — произвольные постоянные, а \( \psi(x) \) определяется из уравнения
\[
\psi''(x) = \frac{f(x) - A^2}{A x + B}
\]
и находится двукратным интегрированием.

Эти семейства решений справедливы для произвольной функции \( f(x) \) и параметра \( k \).

---

Попробуйте искать решения в виде $u=\varphi(x)y^{k+2}+\psi(x)$.

---

Для уравнения

\[
u_{xy}^2 - u_{xx}u_{yy} = f(x) y^k,
\]

где \( f(x) \) — произвольная функция, \( k \) — свободный параметр, получены следующие точные решения, выражаемые в квадратурах или через решения обыкновенных дифференциальных уравнений.

### 1. Случай \( k \neq -2, \, k \neq -1 \)
Решение имеет вид  
\[
u(x, y) = \varphi(x) y^{k+2} + \psi(x),
\]  
где  
\[
\varphi(x) = (\alpha x + \beta)^{-(k+1)}, \quad \alpha, \beta = \text{const},
\]  
а функция \( \psi(x) \) определяется из уравнения  
\[
\psi''(x) = -\frac{f(x)}{(k+2)(k+1)} (\alpha x + \beta)^{\,k+1}
\]  
и находится двукратным интегрированием:  
\[
\psi(x) = -\frac{1}{(k+2)(k+1)} \iint f(x) (\alpha x + \beta)^{\,k+1} \, dx^2 + C_1 x + C_2.
\]

### 2. Случай \( k = -2 \)
Решение имеет вид  
\[
u(x, y) = (A x + B) \ln y + \psi(x), \quad A, B = \text{const},
\]  
где  
\[
\psi''(x) = \frac{f(x) - A^2}{A x + B},
\]  
что также интегрируется в квадратурах:  
\[
\psi(x) = \iint \frac{f(x) - A^2}{A x + B} \, dx^2 + C_3 x + C_4.
\]

### 3. Случай \( k = -1 \)
При произвольной \( f(x) \) решение указанного вида не существует. Если же \( f(x) \equiv 0 \), то уравнение вырождается в \( u_{xy}^2 - u_{xx}u_{yy} = 0 \), и любая функция вида  
\[
u(x, y) = c y + \psi(x), \quad c = \text{const},
\]  
удовлетворяет ему.

Эти семейства решений охватывают все значения параметра \( k \) и произвольную функцию \( f(x) \).