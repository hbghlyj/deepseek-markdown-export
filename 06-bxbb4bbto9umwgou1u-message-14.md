Уравнение имеет вид \( y(x) \, y(a - x) = b^2 \).

### Проверка для функции \( y(x) = -b \cdot \frac{f(x)}{f(a - x)} \):
Вычислим:
\[
y(a - x) = -b \cdot \frac{f(a - x)}{f(a - (a - x))} = -b \cdot \frac{f(a - x)}{f(x)}.
\]
Тогда:
\[
y(x) \cdot y(a - x) = \left(-b \cdot \frac{f(x)}{f(a - x)}\right) \cdot \left(-b \cdot \frac{f(a - x)}{f(x)}\right) = b^2 \cdot \frac{f(x) f(a - x)}{f(a - x) f(x)} = b^2.
\]

Таким образом, функция \( y(x) = -b \cdot \frac{f(x)}{f(a - x)} \) удовлетворяет уравнению.

**Замечание:** Общее решение при \( b \neq 0 \) можно записать в виде:
\[
y(x) = b \cdot \frac{f(x)}{f(a - x)},
\]
где \( f \) — произвольная ненулевая функция, так как знак «минус» можно включить в \( f \) (например, заменив \( f(x) \) на \( -f(x) \)). Тем не менее, явная проверка показывает, что оба выражения являются решениями.